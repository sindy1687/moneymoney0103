# 主題系統模組化文檔

## 📁 檔案結構

```
js/
├── theme.js              # 主載入器 (入口文件)
├── theme-data.js         # 主題數據定義
├── theme-icons.js        # 按鈕圖標配置
├── theme-core.js         # 核心功能模組
├── theme-ui.js           # UI組件模組
├── theme-main.js         # 主入口文件
└── theme-backup-original.js  # 原始備份文件
```

## 🎯 模組說明

### 1. **theme.js** - 主載入器
- 負責按順序載入所有模組
- 保持向後兼容性
- 提供錯誤處理機制

### 2. **theme-data.js** - 主題數據定義
- 包含所有主題的定義
- 主題分類配置
- 背景圖片和卡片圖片配置

```javascript
// 使用方式
const themes = window.ThemeData.themes;
const categories = window.ThemeData.themeCategories;
```

### 3. **theme-icons.js** - 按鈕圖標配置
- 所有主題的按鈕圖標配置
- 原始按鈕圖標備份
- 可愛主題的特殊圖標資源

```javascript
// 使用方式
const buttonIcons = window.ThemeIcons.buttonIcons;
const originalIcons = window.ThemeIcons.originalButtonIcons;
```

### 4. **theme-core.js** - 核心功能模組
- 主題應用和切換功能
- 背景圖片處理
- 按鈕圖標更新
- 自訂主題功能
- 本地存儲管理

```javascript
// 主要函數
window.ThemeCore.getCurrentTheme()
window.ThemeCore.applyTheme(themeId)
window.ThemeCore.updateThemeButtons(themeId)
window.ThemeCore.applyThemeBackgroundImage(imageUrl)
```

### 5. **theme-ui.js** - UI組件模組
- 主題選擇器UI
- 主題卡片創建
- 模態框管理
- 搜索和分類功能
- 視頻控制器

```javascript
// 主要函數
window.ThemeUI.showThemeSelector()
window.ThemeUI.createThemeModal()
window.ThemeUI.renderThemeGrid()
window.ThemeUI.themeVideoController
```

### 6. **theme-main.js** - 主入口文件
- 初始化整個主題系統
- 設置全局兼容性
- 模組載入檢查

## 🚀 使用方式

### 在HTML中引用
```html
<!-- 只需要引用主載入器 -->
<script src="js/theme.js"></script>
```

### 程式化使用
```javascript
// 應用主題
window.ThemeCore.applyTheme('dreamyBlue');

// 獲取當前主題
const currentTheme = window.ThemeCore.getCurrentTheme();

// 顯示主題選擇器
window.ThemeUI.showThemeSelector();

// 應用背景圖片
window.ThemeCore.applyThemeBackgroundImage('image.jpg');
```

## 🔧 添加新主題

### 1. 在 theme-data.js 中添加主題定義
```javascript
{
    id: 'newTheme',
    name: '新主題',
    icon: '🎨',
    buttonIcon: '🖌️',
    preview: 'linear-gradient(...)',
    color: '#color',
    category: 'basic',
    backgroundImage: 'url/to/image.jpg',
    investmentCardImage: 'url/to/image.jpg',
    // ... 其他卡片圖片
}
```

### 2. 在 theme-icons.js 中添加圖標配置
```javascript
newTheme: {
    fab: '🎨',
    navLedger: '📖',
    navWallet: '💰',
    navInvestment: '📈',
    navChart: '📊',
    navSettings: '⚙️'
}
```

### 3. 在 styles.css 中添加CSS樣式
```css
:root[data-theme="newTheme"] {
    --color-primary: #color;
    --color-secondary: #color2;
    /* ... 其他顏色變數 */
}
```

## 🎨 主題分類

目前支援的主題分類：
- **basic** - 經典色彩
- **nature** - 自然風光
- **cosmic** - 宇宙星空
- **dark** - 深色主題
- **anime** - 動漫風格
- **wealth** - 財富金錢
- **cute** - 可愛風格
- **fantasy** - 奇幻風格

## 📱 響應式設計

所有主題都支援響應式設計，自動適配不同螢幕尺寸：
- 桌面電腦 (>768px)
- 平板電腦 (768px-480px)
- 手機 (<480px)

## 🔄 向後兼容

模組化後保持完全的向後兼容性：
- 所有原有的全局函數仍然可用
- 原有的HTML結構無需修改
- 原有的CSS樣式保持不變

## 🧪 測試

使用 `theme-test.html` 進行測試：
1. 在瀏覽器中打開 `theme-test.html`
2. 查看模組載入狀態
3. 點擊測試按鈕驗證功能
4. 測試主題選擇器

## 📝 注意事項

1. **載入順序**：模組按特定順序載入，不要修改順序
2. **依賴關係**：後續模組依賴前面的模組
3. **全局變數**：所有模組都掛載到 `window` 對象下
4. **錯誤處理**：載入失敗時會在控制台顯示錯誤信息
5. **性能優化**：模組採用異步載入，不會阻塞頁面渲染

## 🎯 優勢

### 模組化前
- 單一文件 1400+ 行
- 難以維護和擴展
- 功能耦合度高
- 代碼重複

### 模組化後
- 6個獨立模組，職責清晰
- 易於維護和擴展
- 低耦合，高內聚
- 代碼復用性高
- 支援獨立測試
- 便於團隊協作

## 🔮 未來擴展

模組化結構為未來擴展提供了良好的基礎：
- 可以輕鬆添加新的主題類型
- 支援插件式主題擴展
- 可以實現主題的動態載入
- 支援主題的版本管理
- 可以實現主題的在線更新
